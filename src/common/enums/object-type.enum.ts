/**
 * Copyright (c) Overnight
 */

enum ObjectType {
  Account = 'account',
  AuthToken = 'auth-token',
  Image = 'image',
  List = 'list',
  Neighborhood = 'neighborhood',
  Organization = 'organization'
}

export { ObjectType }
